agent:
  enabled: true
collector:
  enabled: true
query:
  enabled: true
  podLabels:
    vkpr: "true"
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - jaeger.localhost
provisionDataStore:
  cassandra: true
