ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "true"
  hosts:
    - host: domain
      paths: ["/"]
  pathType: Prefix
  tls:
    - hosts:
        - domain
      secretName: whoami-cert
