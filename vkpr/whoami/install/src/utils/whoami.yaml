ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "true"
  pathType: Prefix
  hosts:
    - host: domain
      paths: ["/"]
  tls:
  - hosts:
    - domain
    secretName: whoami-cert