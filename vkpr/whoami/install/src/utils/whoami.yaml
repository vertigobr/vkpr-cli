ingress:
  enabled: true
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "true"
  hosts:
    - host: ""
      paths: ["/"]
  pathType: Prefix
  tls:
    - hosts:
        - ""
      secretName: whoami-cert
