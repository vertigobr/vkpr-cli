  apiVersion: networking.k8s.io/v1
  kind: Ingress
  metadata:
    name: loki
    namespace: vkpr
  spec:
    ingressClassName: nginx
    rules:
      - host: loki.localhost
        http:
          paths:
            - backend:
                service:
                  name: 
                  port:
                    number: 3100
              path: /
